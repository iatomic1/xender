import { onMessage } from "@/lib/messaging";

export default defineBackground(() => {
  // The message handler function can be async
  // onMessage("CONNECT-WALLET", async (message) => {
  // });
});
