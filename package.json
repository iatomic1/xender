{
  "name": "xender",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "^9.1.7"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "lint": "cd xender-frontend/ && bun lint",
    "check-types": "cd xender-frontend/ && bunx tsc --noEmit",
    "prepare": "husky"
  },
  "lint-staged": {
    "**/*.{ts,tsx}": "bun lint"
  }
}
